#include "bits/stdc++.h"

int main (int argc, char *argv[]) {
  int tc; cin>> tc;
  while (tc--) {
    int n; cin>> n;
    vector<int> hs(n);
    vector<int> w(n,0);

    for (int i = 0; i < n; i++) {
      cin >> hs[i];
    }
    
    for (int i = 0; i < n; i++) {
      int maxHeight = hs[i];


      for (int j = 0; j < n; j++) {
        
      }
    }
  }
  return 0;
}
