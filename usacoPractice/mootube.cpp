#include "bits/stc++.h"
using namespace std;

int main (int argc, char *argv[]) {
  int N,Q;cin>>N>>Q;

  vector<vector<int>> nodes(N);
  for (int i = 0; i < N-1; i++) {
    int f,s;
    cin>>f>>s;
    nodes[f].push_back();


  }
  return 0;
}
