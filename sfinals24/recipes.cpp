#include <bits/stc++.h>
using namespace std;

int main (int argc, char *argv[]) {
  int n,m,k;cin>>n>>m>>k;
  string al; cin>>al;
  string pref;cin>>pref;
  vector<string> rcps(n);

  for (int i = 0; i < n; i++) {
    cin>>rcps[i];
  }
  return 0;
}
